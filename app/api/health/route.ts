/**
 * GET /api/health
 * Health check endpoint - minimal info, no config leaks
 */

import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
  });
}
